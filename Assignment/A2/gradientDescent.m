function [beta_path, beta, L_history] = gradientDescent(X, Y, beta, alpha, stop_crit)
    %GRADIENTDESCENT Performs gradient descent to learn beta
    %   beta = GRADIENTDESCENT(X, Y, beta, alpha, stop_crit) updates beta by 
    %   taking gradient steps with learning rate alpha until convergence
    %   (wrt some criterion)
    
    % Initialize some useful values
    n = length(Y); % number of training examples
    L_history = 0;
    beta_path = beta(:)';
    iter = 1;
    % Convergence criteria are up to you
    
    
    
    
    
    
    
end
