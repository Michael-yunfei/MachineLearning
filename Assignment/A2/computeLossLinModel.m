function L = computeLossLinModel(X, Y, beta)
%COMPUTECOST Compute loss for linear regression
%   L = COMPUTECOST(X, Y, beta) computes the loss when using beta as the
%   parameter for linear regression to fit the data points in X and Y
%   Instructions: Compute the loss when using a particular choice of beta
%               You should set L to the loss.

end
